// Helper to map theme names to their actual style objects
const themes = {
    light: lightTheme,
    dark: darkTheme,
    ocean: oceanTheme,
    forest: forestTheme,
    minimalist: minimalistTheme,
    highContrast: highContrastTheme,
};

export default {themes};